// Q9=Write a simple JavaScript program that checks if a person is 
// eligible for a CNIC 
var names = "sumbul";
var age = 23;
if (age >= 18) {
    console.log("Congratulations!" + names + " You are eligible to apply for a CNIC");
}
else {
    console.log("Sorry" + names + " You are not eligible to apply for a CNIC yet.");
}
